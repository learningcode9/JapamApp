{
    "buildCommand": "expo export -p web && node scripts/postprocess-web-html.js && cp public/*.html dist/",
    "outputDirectory": "dist",
    "devCommand": "expo",
    "cleanUrls": true,
    "framework": null,
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "/"
      }
    ]
  }
